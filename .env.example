# Required (at least 32 chars)
SESSION_SECRET=replace-with-a-long-random-string

# Preferred auth mode (global password)
DECK_PASSWORD=replace-with-password

# Optional fallback auth mode (JSON array of users)
# DECK_USERS_JSON=[{"user":"pandora","pass":"secret"}]